#include<sidio.h>
int main()
{
char c;
printf("enter the character");
scanf("%c",&c);
if((c>='a'7&c<='z')//(c>='A'&&c<='Z'))
printf("%c is an alphabet",c);
else
printf("%c is not an alphabet",c);
return0;
}
